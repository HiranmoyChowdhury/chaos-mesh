apiVersion: kubedb.com/v1
kind: Redis
metadata:
  name: rd-std
  namespace: chaostesting
spec:
  version: 8.2.2
  storageType: Durable
  storage:
    storageClassName: "standard"
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
  deletionPolicy: WipeOut